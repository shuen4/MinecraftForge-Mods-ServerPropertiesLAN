2.70a (less testing)
+ fix /ban /ban-ip /banlist /pardon /pardon-ip commands display permission error on 1.13 - 1.15
+ add missing /kick command

2.70 (less testing)
+ add server option (allow-nether enable-command-block enable-status[1.16+] require-resource-pack[1.17+] hide-online-players[1.18+])
+ add mod option send-server-status (control whether to send server status after joining)

2.69
+ fix setting resource pack crash on 1.13 - 1.16 (prevent resource pack prompt GUI display for host so won't crash, host need to manually apply the resource pack)
+ remove max-view-distance (require modify IntegratedServer tick() to fix)

2.68
+ split code

2.67b
+ fix issue #1 (tested only Forge 1.17.1-37.0.104)

2.67a
+ optimize text display
+ add code comments
+ disable if mod is running on forge server

2.67
+ will not open server port on player join

2.66b
+ fix Whitelist not working (move to /whitelist command #require allow-cheat on)
+ fix client with mod join server will make it unplayable

2.66a
+ fix LAN port text to actual port instead of port 0

2.66 
+ initialize version for mc 1.13+
